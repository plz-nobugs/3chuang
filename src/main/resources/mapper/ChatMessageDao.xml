<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.ChatMessageDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.ChatMessageEntity" id="chatMessageMap">
        <result property="id" column="id"/>
        <result property="sessionId" column="session_id"/>
        <result property="senderType" column="sender_type"/>
        <result property="senderId" column="sender_id"/>
        <result property="senderName" column="sender_name"/>
        <result property="content" column="content"/>
        <result property="isRead" column="is_read"/>
        <result property="addtime" column="addtime"/>
    </resultMap>

    <select id="selectListView" resultType="com.entity.ChatMessageEntity">
        SELECT * FROM chat_message
        <where>
            <if test="ew != null">
                ${ew.sqlSegment}
            </if>
        </where>
    </select>

    <select id="selectView" resultType="com.entity.ChatMessageEntity">
        SELECT * FROM chat_message WHERE id = #{id}
    </select>

</mapper>