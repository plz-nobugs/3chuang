<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.LockerstatusDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.LockerstatusEntity" id="lockerstatusMap">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="longitude" column="longitude"/>
        <result property="latitude" column="latitude"/>
        <result property="total" column="total"/>
        <result property="used" column="used"/>
        <result property="description" column="description"/>
    </resultMap>

    <select id="selectListVO"
            resultType="com.entity.vo.LockerstatusVO" >
        SELECT * FROM lockerstatus lockerstatus
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>

    <select id="selectVO"
            resultType="com.entity.vo.LockerstatusVO" >
        SELECT lockerstatus.* FROM lockerstatus lockerstatus
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>

    <select id="selectListView"
            resultType="com.entity.view.LockerstatusView" >
        SELECT lockerstatus.* FROM lockerstatus lockerstatus
        <where> 1=1 ${ew.sqlSegment}</where>
    </select>

    <select id="selectView"
            resultType="com.entity.view.LockerstatusView" >
        SELECT * FROM lockerstatus lockerstatus <where> 1=1 ${ew.sqlSegment}</where>
    </select>

</mapper>